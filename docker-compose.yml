services:
  sofia-brain:
    build: .
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      easypanel-biex:
        aliases:
          - sofia-brain
          - brain-api
    restart: unless-stopped

networks:
  easypanel-biex:
    external: true
